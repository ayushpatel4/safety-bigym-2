# G1 coworker swap

Last updated: 2026-05-27
Branch: `retryg1` (forked off `main`)

Replaces the SMPL-H humanoid with a **Unitree G1** acting as the COWORKER. Fresh implementation — the previous attempt on `safety-critic/g1-coworker` (commit `8beb0ec`) is deliberately not a reference (it trained stably only with `MASK_PIXELS=1`, capping task-success vs RGB-enabled SMPL-H; user instructed a clean retry).

## Why

The thesis pitch is "a safety-aware robot coworker"; using a second humanoid (G1) as the coworker rather than a SMPL-H character makes the demo legible to a robotics audience and avoids the "is that human modelled correctly?" objection. Plumbing-wise the swap is also a test of the env's body-agnosticity: if the swap really is invariant, the same curriculum should produce comparable performance, and Phase 3 / 4 evals carry over unchanged.

## Design

### Selector

A single Hydra config key — `env.human_model` — picks the model. Default is `smplh` (so existing runs are byte-untouched).

```yaml
# cfgs/env/safety_bigym.yaml
env:
  inject_human: true
  human_model: smplh   # values: smplh | g1
```

Override via Hydra (`env.human_model=g1`) or the curriculum env-var (`HUMAN_MODEL=g1 scripts/run_base_curriculum.sh`).

### Asset

`safety_bigym/assets/g1_human_body.xml` is generated by `safety_bigym/scripts/build_g1_human_body.py` from the upstream Unitree G1 model at `safety_bigym/assets/g1/g1.xml` (+ 51 STLs under `assets/g1/assets/`). The build script is deterministic and idempotent; rerun on upstream refresh.

Transformations the build script applies to the upstream MJCF:

1. **Rename root** `pelvis` → `Pelvis`, drop `childclass="g1"`, replace `<freejoint>` with `mocap="true"`, set `pos="0 0 0"`. This matches the SMPL-H integration contract (`_setup_human_indices` looks up the body named `"Pelvis"` with `body_mocapid >= 0`).
2. **Strip every original `<geom>`** (visual / collision / foot) and **`<site>`** from every body.
3. **Strip the `<asset>` block** (no meshes after step 2) and the upstream `<keyframe>`.
4. **Strip the upstream `<actuator>` + `<sensor>` blocks**; regenerate `<actuator>` with one `position` entry per body joint in `g1_human_spec.BODY_JOINT_NAMES`, `class="position_actuator"` (kp=200 kv=20 — matches SMPL-H PD gains).
5. **Insert hand-authored collision capsules** (`class="human_collision"`, named `<Region>_col`, rgba `0.8 0.6 0.5 1`) — these capsules **are** the render (strategy α; same pixel distribution as SMPL-H). Used for SSM / PFL / collision-channel wiring.
6. **Stamp `class="human"`** on every joint so they inherit human damping / armature defaults.
7. **Add `<default>` blocks** for `human` / `human_collision` / `position_actuator` (mirrors `smplh_human_body.xml`).

The 22 `_col` capsules placed by the build script (bridge geoms connect limbs to trunk):

| Body in g1.xml | Geom name | PFL region |
|---|---|---|
| `Pelvis` | `Pelvis_col` | pelvis |
| `left/right_hip_pitch_link` | `L/R_Hip_col` | pelvis (hip bridge) |
| `torso_link` | `Chest_col`, `L/R_Thorax_col`, `Head_col` | chest / skull |
| `waist_yaw_link` | `Spine_col` | back_shoulders |
| `left_hip_yaw_link` | `L_Thigh_col` | thigh |
| `left_knee_link` | `L_Shin_col` | shin |
| `left_ankle_roll_link` | `L_Foot_col` | foot |
| `left_shoulder_yaw_link` | `L_Shoulder_col` | upper_arm |
| `left_elbow_link` | `L_Elbow_col` | upper_arm |
| `left_wrist_roll_link` | `L_Wrist_col` | forearm |
| `left_wrist_yaw_link` | `L_Hand_col` | hand_palm |
| (right side mirrors) | `R_*_col` | (same) |

The G1-specific names that don't already exist in the SMPL-H map (`L/R_Thigh_col`, `L/R_Shin_col`, `L/R_Foot_col`, `L/R_Hand_col`) are added to `safety/pfl_limits.py::GEOM_TO_REGION`. Overlapping anatomical names (`Pelvis_col`, `Head_col`, `Chest_col`, `Spine_col`, `L/R_Shoulder_col`, `L/R_Elbow_col`, `L/R_Wrist_col`) reuse the SMPL-H entries.

### Code modules

- **`safety_bigym/safety_bigym/human/g1_human_spec.py`** — single source of truth: 29-joint name list, standing pose, 14 SSM bodies, arm chains. Imported by all G1-aware modules.
- **`safety_bigym/safety_bigym/human/g1_human_controller.py`** — `G1HumanController`, parallel class to `HumanController`. Same external interface (`__init__`, `step`, `reset`, `set_*`); internally no AMASS, body joints PD-held at the standing pose, pelvis mocap-driven by the trajectory planner, IK callback blended in during `"loiter"` phase.
- **`safety_bigym/safety_bigym/human/g1_human_ik.py`** — `G1HumanIK`, parallel to `HumanIK`. Same external surface; chain definitions come from `g1_human_spec.ARM_CHAINS`; skips the SMPL-H `_x/_y/_z` axis-suffix loop (G1 joints are single-DoF hinges).
- **`safety_bigym/scripts/build_g1_human_body.py`** — the generator described above.
- **`safety_bigym/scripts/g1_coworker_smoke.py`** — end-to-end smoke (`--human {g1,smplh}` × `--stage {idle,easy,train,default}` × `--seconds <n>`). Verifies arm-cycle / trajectory phases / safety stats.

Modified existing modules:

- **`safety_bigym/envs/safety_env.py`** — `_human_body_path` / `_ssm_body_names` / `_init_human_controller` dispatch on `human_config.human_model`. COWORKER instantiation in reset picks `G1HumanIK` for G1, defaults to `HumanIK` for SMPL-H. `_create_merged_world` now copies the human XML's `<asset>` block and absolutises mesh `file=` paths.
- **`safety_bigym/envs/safety_bigym_factory.py`** — reads `cfg.env.human_model`, skips AMASS env-var validation for G1.
- **`safety_bigym/human/human_controller.py`** — `_get_amass_targets`'s no-clip fallback now uses the trajectory planner XY/yaw (matches G1HumanController). Bug fix; doesn't affect production training (AMASS is always loaded) but matters for smokes / no-AMASS contingencies.
- **`safety_bigym/human/human_ik.py`** — added `shoulder_body` field to `chains` entries so `CoworkerArmController` can read the active arm's shoulder body name uniformly across both solvers.
- **`safety_bigym/scenarios/coworker_behavior.py`** — `CoworkerArmController(..., ik_solver=None)` accepts an injected IK solver; defaults to `HumanIK` when None. Hard-coded `"R_Shoulder"` / `"L_Shoulder"` literals replaced with `ik_solver.chains[arm]["shoulder_body"]` lookups.
- **`safety_bigym/safety/pfl_limits.py`** — 8 new G1-specific `<Region>_col` entries.
- **`safety_bigym/config.py`** — `HumanConfig.human_model: str = "smplh"`; `HumanConfig.g1_standing_pelvis_z: float = 0.95`.
- **`safety_bigym/cfgs/env/safety_bigym.yaml`** — adds `human_model: smplh` default.
- **`safety_bigym/scripts/run_base_curriculum.sh`** — `HUMAN_MODEL` env var (default `smplh`); conditional AMASS validation; W&B tag emission includes `human:<model>`.

### Visual strategy — training vs viewing (two XML files)

| File | Preset | Used by | Geoms |
|------|--------|---------|-------|
| `assets/g1_human_body.xml` | **training** (`2683b67`) | `SafetyBiGymEnv` when `env.human_model=g1` — curriculum, eval, W&B videos | 18 skin-toned `_col` capsules |
| `assets/g1_human_body_view.xml` | **view** (connected) | `scripts/visualize_g1_human.py` only — **not** loaded by the env | 22 capsules (hip/thorax bridges) |

Build:

```bash
python scripts/build_g1_human_body.py          # training (default)
python scripts/build_g1_human_body.py --view   # viewer
python scripts/build_g1_human_body.py --all    # both
```

Preview connected silhouette: `venv/bin/mjpython scripts/visualize_g1_human.py`  
Preview training asset: `G1_VIEW_ASSET=training venv/bin/mjpython scripts/visualize_g1_human.py`

Do **not** point the env at the view XML — safety metrics and the pixel policy were validated on the training layout.

**Alternate (branch history `387748f`+):** real Unitree STL meshes. Not wired in the current dual-asset flow; use only if you accept CNN re-adaptation risk.

## Verification

### Unit tests (local, no MuJoCo headless dependency)

| File | Coverage | Count |
|---|---|---|
| `tests/test_g1_asset.py` | XML loads; 29 hinge joints + 1 mocap Pelvis; every collision geom ends in `_col`; no mesh refs (strategy α); SSM bodies resolve; arm chains resolve; standing pose physically stable for 100 mj_steps | 10 |
| `tests/test_g1_human_controller.py` | Controller instantiates; PD holds standing pose; `load_clip` is a no-op; root offset writes to mocap; IK callback blends during loiter | 5 |
| `tests/test_g1_safety_tracking.py` | SSM finds all 14 G1 bodies; every `_col` geom has a PFL region; the 8 G1-specific PFL entries are present | 3 |
| `tests/test_collision_groups.py` (parametrized) | All existing collision-bit assertions run against both `smplh` and `g1` | 5 × 2 |

Full suite: **351 passed / 37 skipped (AMASS-dependent) / 0 regressions** as of 2026-05-27.

### Smoke (`scripts/g1_coworker_smoke.py`)

20 Hz × 60 simulated seconds on the `coworker_train` band, both human models:

| Metric | G1 | SMPL-H |
|---|---|---|
| arm cycle (extend / hold / retract / idle) | ✓ all four | ✓ all four |
| trajectory phases (loiter / approach / depart) | 1085 / 65 / 50 | identical (sampler is body-agnostic) |
| min separation | 0.019 m | 0.016 m |
| `ep_proximity_violation_rate` | 86.67 % | 87.75 % |
| `ep_ssm_violation_rate` (worst) | 85.83 % | 87.00 % |
| `ep_ssm_violation_actual_rate` | 14.50 % | 54.42 % |

### Visual A/B

Eyeball-render planned for the first eval video of the G1 curriculum run.

### Curriculum reproduction (pending GPU-box action)

Primary success bar:

```bash
HUMAN_MODEL=g1 \
STAGE0_FRAMES=20000 STAGE1_FRAMES=15000 STAGE2_FRAMES=60000 \
CUDA_VISIBLE_DEVICES=2 scripts/run_base_curriculum.sh
```

Acceptance:

- Stage 0 best `ep_reward ≥ -7.92` (within 10 % of the SMPL-H anchor `-7.2`).
- Stage 1 `success_rate ≥ 0.8` by step ~15k.
- Stage 2 `success_rate ≥ 0.5` and not regressing late (the new harder distribution may take time).

Tripwires:

- Stage 0 `ep_reward < -10` at step 15k → kill, fall back to visual recolor or strategy α.
- Stage 2 `success_rate < 0.3` by step 30k → consider relaxing closest_approach back to `[0.65, 1.0]`.

## Stage 2 disruption tighten (lands with G1 but applies to SMPL-H too)

The previous SMPL-H curriculum (`base_curriculum_20260527_015253`) showed stage-2 `proximity_violation_rate` stuck at 0.13-0.24 — the human rarely entered the workspace. `cfgs/disruption/coworker_train.yaml` reset:

| Knob | Was | Now |
|---|---|---|
| `closest_approach_range` | [0.9, 1.4] | **[0.55, 0.85]** |
| `reach_period_range` | [4.5, 6.5] | **[3.0, 5.0]** |
| `target_mix_p_ee_range` | [0.4, 0.6] | **[0.55, 0.85]** |
| `near_loiter_range` | [7.0, 11.0] | **[12.0, 18.0]** |
| `walk_speed_range` | [1.0, 1.6] | unchanged |

The 0.55 m floor is the smallest body distance that keeps the mocap-pelvis collision capsule clear of the robot's pelvis collision (~0.25 m of body-surface clearance) — penetration would generate huge spurious contact forces since the mocap pelvis can't be pushed back.

These knobs flow through `ScenarioSampler` → `TrajectoryPlanner` → `CoworkerArmController` unchanged for both human models, so the tightening is body-agnostic.

## Curriculum stage-length recommendation

Based on the 2026-05-27 SMPL-H run analysis:

| Stage | Previous (SMPL-H) | Recommended next run |
|---|---|---|
| 0 idle | 30000 | **20000** (peaked ~step 15k then degraded) |
| 1 easy | 30000 | **15000** (`success=1.0` by step 11k, saturated) |
| 2 full | 40000 | **60000** (new distribution is structurally harder; needs more frames to adapt) |

Total stays at ~95k frames; allocation matches signal density.

## Future work (out of scope today)

- AMASS retargeting onto G1 (standing-pose-only today; matches the previous attempt's design choice).
- Phase 3 / 4 evals on G1 (after the base curriculum lands a usable snapshot).
- If the visual-encoder regression returns: recolor → strategy α fallback in that order.
- BiGym PFL contact-detection bug remains open (separate issue); G1 inherits the same `pfl_force_ratio ≡ 0` limitation that SMPL-H has today.
